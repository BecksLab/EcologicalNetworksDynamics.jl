# `include` this file to reassure JuliaLS: these are keywords for the macros.
#  https://github.com/julia-vscode/StaticLint.jl/issues/381#issuecomment-2361743645
if (false)
    #! format: off
    (
    local
        E,
        V,
        Y,
        dense,
        depends,
        edges,
        get,
        graph,
        nodes,
        property,
        ref,
        ref_cached,
        requires,
        requires,
        write!,

        var""
    )
    #! format: on
end
